using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DrinkingSpot : MonoBehaviour
{
    public bool occupied;

    // Start is called before the first frame update
    void Awake()
    {
        occupied = false;
    }
}
